name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: Print greeting
        run: print('Hello World!')
      - name: Print goodbye
        run: print('Done - bye')
